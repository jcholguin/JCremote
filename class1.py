#First classclass Clase1:    Lada_jalisco = "33"    Lada_sonora = "64"    __password = "passw0rd"            def __init__(self, nombre = None, apellido_p = None, apellido_m = None, place = "Pending"):        self.nombre = nombre        self.apellido_paterno = apellido_p        self.apellido_materno = apellido_m        self.place = place        def __str__(self):        return f"La persona se llama {self.nombre} {self.apellido_paterno} {self.apellido_materno}, recide en {self.place}"        def modify_tel(self, numero):        if self.place == "Jalisco":            self.telefono = self.Lada_jalisco + numero        elif self.place == "Sonora":            self.telefono = self.Lada_sonora + numero        else:            print("Place no valid or missing place")        print(f"{self.nombre} {self.apellido_paterno} your complete phone is {self.telefono}")            def atributos(self):        print(f"{self.nombre} {self.apellido_paterno} tiene {len(self.__dict__.keys())} attributos")        while(True):            option = input("Quieres imprimir los attributos (y/n)? ")            if option == 'y':                self.imprimir_atributos()            elif option == 'n':                break            else:                print("Invalid option")        def imprimir_atributos(self):        for n, i in enumerate(self.__dict__.keys()):            print(f"{n+1}) {i}: {self.__dict__[i]}")        @classmethod    def __modify_pass(cls, actual = None, new = None):        if actual == None:            raise ValueError("Actual password missing")            return        else:            if actual == cls.__password:                cls.__password = new                print("\tPassword changed")                print(f"Password {actual} is not longer available")                print(f"New password is {cls.__password}")                    def print_history(self):        print("+You want to see the whole company bank accounts?")        option = input("Password: ")        if option == self.__password:            print("\tSales company history .....")        else:            raise ValueError("¡Wrong password!")        @classmethod    def modify_password(cls):        print("- Modify actual password")        option = input("Password: ")        if option == cls.__password:            new_pass = input("Write new password: ")            cls.__modify_pass(option,new_pass)        else:            raise ValueError("¡Wrong password!")                persona1 = Clase1("Juan", "Carlos", "Holguin", "Jalisco")persona2 = Clase1("Luis", "Alonso", "Holguin", "Sonora")"""print(persona1)print(persona2)persona1.modify_tel("17897538")persona2.modify_tel("31255751")#persona1.atributos()persona1.print_history()Clase1.modify_password()"""class Empresa2(Clase1):        def __init__(self, nombre, apellido_p, apellido_m, place, sueldo, edad):        Clase1.__init__(self, nombre, apellido_p, apellido_m, place)        self.sueldo = sueldo        self. edad = edad        def __str__(self):        return Clase1.__str__(self) + ", su sueldo {} y su edad {}".format(self.sueldo,self.edad)            persona3 = Empresa2("Luis","Holguin","Zazueta","Obson",15000,26)print(persona3)                """def Add():    nombre = input("Nombre: ")    apellido_p = input("Apellido paterno: ")    apellido_m = input("Apellido materno: ")    lugar = input("Recidencia: ")    persona = Clase1(nombre, apellido_p, apellido_m, lugar)def Print():    def Add_phone():    passdef Check_history():    passdef Change_pass():    passMenu_options = {"1":Add, 2:Print, 3:Add_phone, 4:Check_history, 5:Change_pass}while(True):        print(\\t** Company system **1) Add employee2) Print employees info3) Add phone numer to an employee4) Check company history5) Change password6) Exit)    option_menu = input("Which option: ")    if option_menu in Menu_options.keys():        Menu_options[option_menu]()    elif option_menu == '6':        break    else:        print("¡¡¡Invalid option!!! \n")"""                